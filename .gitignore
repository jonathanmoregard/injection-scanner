__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venv-*/
.pytest_cache/
build/
dist/
.coverage

# Library — consumers manage their own lock; we don't pin
# dev-time deps here.
uv.lock

# Working notes live in userspace (~/.local/state/claude-tasks/<repo>/);
# `tasks` is a symlink to them and must never be committed.
tasks
